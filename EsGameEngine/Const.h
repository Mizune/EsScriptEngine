#pragma once
#include <string>

using namespace std;
class Const
{
public:
	Const();
	~Const();
	int static const DEFAULT_SCREEN_WIDTH = 1280;
	int static const DEFAULT_SCREEN_HEIGHT = 720;
	string static const DEFAULT_BG_FILENAME;
	
};

